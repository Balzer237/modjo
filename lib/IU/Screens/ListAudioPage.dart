import 'package:flutter/material.dart';

class Listaudiopage extends StatelessWidget {
  const Listaudiopage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("je suis la"),);
  }
}
